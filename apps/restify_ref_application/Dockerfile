# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build RestifyReferenceApplication`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t RestifyReferenceApplication`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system RestifyReferenceApplication && \
          adduser --system -G RestifyReferenceApplication RestifyReferenceApplication

COPY dist/apps/restify_ref_application RestifyReferenceApplication/
RUN chown -R RestifyReferenceApplication:RestifyReferenceApplication .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix RestifyReferenceApplication --omit=dev -f install

CMD [ "node", "RestifyReferenceApplication" ]
